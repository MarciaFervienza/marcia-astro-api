flask>=3.0,<4.0
gunicorn>=21.0,<24.0
anthropic>=0.40,<2.0
openai>=1.40,<3.0
pinecone>=5.0,<8.0
requests>=2.31,<3.0
reportlab>=4.0,<5.0
# svglib is pure-Python (uses reportlab graphics primitives) — Railway-safe,
# no system libraries required. cairosvg would render higher-fidelity output
# but depends on libcairo which is not guaranteed on Railway's nixpacks image.
svglib>=1.5,<2.0
